{% extends 'base.html.twig' %}

{% block title %}{{ controller_name }}{% endblock %}

{% block body %}
<div class="card-columns dark">
    {% for categorie in categories %}
	<a href="{{ path('main_categories_show', {'slug': categorie.slug}) }}">
  	<div class="card">
		<div class="card-body">
    <i class="fa fa-{{categorie.icon}}" aria-hidden="true"></i> 
			<h5 class="card-title">{{categorie.name}}</h5>
			<small class="text-muted">
          {% if app.user %}
            <a href="{{path('admin_categories_edit',{'id': categorie.id})}}">Modifier</a>
          {% endif %}</small>
			<small class="text-muted">{{categorie.adverts | length}} annonces • le 02/06/2020</small>
		</div> 
	</div>
	</a>
    {% endfor %}
</div>
{% endblock %}
